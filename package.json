{
  "name": "terkelg",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "pnpm stats && wrangler deploy",
    "format": "prettier --write .",
    "stats": "node --loader tsm --env-file .env scripts/stats.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "@types/node": "^20.10.0",
    "tsm": "^2.3.0",
    "typescript": "^5.3.2",
    "wrangler": "^3.17.1"
  },
  "dependencies": {
    "esbuild": "^0.19.8",
    "prettier": "^3.1.0"
  }
}
